# Optional: dbt_utils gives you handy test/macros (e.g. unique_combination_of_columns,
# accepted_range). Uncomment the block below and run `dbt deps` to use it.
# The base project runs fine without it.
#
# packages:
#   - package: dbt-labs/dbt_utils
#     version: [">=1.1.0", "<2.0.0"]
